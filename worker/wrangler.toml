name = "hkfc-api"
main = "src/index.ts"
compatibility_date = "2026-07-04"
account_id = "ff3db45432f5be42c0bfd08d1f65b863"

[vars]
AIRTABLE_BASE_ID = "appG6amyHthm3Nnde"
ALLOWED_ORIGIN = "https://hkfc-squad-selection.squad-selections.workers.dev"

SUPABASE_URL = "https://rttbghxnacfrhjzzjwnh.supabase.co"
SUPABASE_ANON_KEY = "sb_publishable_HbNBcTHob3ZmXkYGWd8IlA_K_TI6UMk"
[triggers]
# Automatic re-registration reconciliation scan.
# Cadence: daily at 02:00 Asia/Hong_Kong (18:00 UTC) - shortly after the
# overnight HKHA results sync window, before coaches start selecting squads.
# Runs in dry-run mode unless the AUTO_REGISTRATION_ENABLED Worker var is
# set to "true"; see README "Automatic Re-registration" for activation steps.
crons = ["0 18 * * *"]
